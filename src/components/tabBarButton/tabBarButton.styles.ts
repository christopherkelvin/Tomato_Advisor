import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tabBarText: {
    marginTop: 1,
  },
  tabBarContainer: {
    display: "flex",
    flex: 1,
    flexDirection: "row",
    gap: 4,
    textAlign: "center",
    paddingVertical: 20,
    paddingHorizontal: 12,
  },
});