import { StyleSheet } from "react-native";
import { originalColor } from "../authLayout/authWrapper.styles";
export const themeColor = "#2e8b57";
export const styles = StyleSheet.create({
  container: {
    paddingTop: 50,
    height: "100%",
    backgroundColor: themeColor,
    // opacity: 0.7,
  },
});
